import type { Meta, StoryObj } from '@storybook/react';

// Placeholder - stories will be implemented in later steps
const meta: Meta = {
  title: 'KanbanBoard',
  component: () => null,
};

export default meta;
type Story = StoryObj<typeof meta>;

export const Placeholder: Story = {
  render: () => <div>Stories will be implemented in Step 2</div>,
};

